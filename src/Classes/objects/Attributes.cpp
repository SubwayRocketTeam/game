#include "pch.h"
#include "Attributes.h"

Attributes::Attributes()
:maxHp(1), maxMp(0),
hpRegen(0), mpRegen(0),
attack(0), defence(0),
attackDelay(1), moveSpeed(8),
criticalRate(0), hitRate(1), evadeRate(0)
{
}